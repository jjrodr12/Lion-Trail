package edu.psu.liontrail.util;

import java.util.Collection;
import java.util.Iterator;
//import edu.psu.liontrail.data.GradedClass;

public class GpaCalc {
  // public static double CalculateGpa(Collection<GradedClass> classes) {
  // if (classes != null && classes.size() > 0) {
  // Iterator<GradedClass> itr = classes.iterator();
  // Double gradeOfClass;
  // Double sumOfGrade = 0.0;
  // Double creditHoursAssigned;
  // Double counter = 0.0;
  // int hoursCounter;
  //
  //
  // while (itr.hasNext()) {
  // gradeOfClass = itr.next().getGradeVal();
  // creditHoursAssigned = itr.next().getCreditHours();
  // if (gradeOfClass != null && gradeOfClass > 0.0 && creditHoursAssigned != null
  // && creditHoursAssigned > 0.0) {
  // sumOfGrade = sumOfGrade + (gradeOfClass * creditHoursAssigned);
  // counter = counter + creditHoursAssigned;
  // }
  // }
  // Double calculatedGpa = sumOfGrade / counter;
  // return calculatedGpa;
  // }
  //
  // return 0;
  // }
  public static double CalculateGpa() {
    return 3.0;
  }

}